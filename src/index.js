// Import the UI module, which handles the user interface.
import UI from './modules/UI';

// When the DOM content is loaded, start the game UI.
document.addEventListener("DOMContentLoaded", () => {
    UI.startGameAppUI(); // Start the user interface for our game application.
});

